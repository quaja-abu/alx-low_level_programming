#include "holberton.h"

/**
 * add - to add to integer numbers
 * @a: an integer argument to hold first value
 * @b: an integer argument to hold second value
 * Return: sum
 */
int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
